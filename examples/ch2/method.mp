# 함수를 가리키는 변수들
    foo = array
    goo = foo
    too = goo

# (3, 4) 크기의 배열을 만들고, 2를 더한다.
    potato = too(3, 4) + 2

# 배열을 파이썬처럼(Pythonic) 인덱싱할 수 있습니다.
    k = potato(0, 1:3) + 3

    print potato, k
